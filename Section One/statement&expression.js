3 + 4; //is an expression that produces a value
true && false; //is an expression that produces a value

if (23 / 10) {
  console.log("true"); //this is a statement because it is part of a control structure and produces an action
}

//In template literals, it can only contain expressions and not statements
const me = "Husayn";
console.log(`${me} would be ${2025 - 1991} years old by September 27.`); //contains expressions "me" and "2025 - 1991" and produces a value
